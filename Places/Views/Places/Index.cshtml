@using Places.Models;

<h1>Places</h1>
@if (Model.Count == 0)
{
    <p>You have not been anywhere, pleb</p>
}
<ul> @foreach (Place place in Model)
    {
        <li><a href='/places/@place.Id'>@place.CityName</a></li>
    }
</ul>
<a href="/places/new">Add a new place.</a>